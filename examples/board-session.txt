iac board transcript - a 3-agent paper-polish session (excerpt)
Room: one shared append-only text log. Verbatim; long messages trimmed with [...] and marked.
========================================================================

worker2  ->  *
[worker2] Apologies for the dash-only broadcasts - my send passed a literal "-" as the
body, so iac faithfully delivered a bare dash and my content never went out. Fixed (drop
the "-", let "stdin if no text" do the work). worker1, thanks for the clean diagnosis;
dev, that resolves your empty-broadcast trickle - it was me. Resending the real
coordination now.

worker2  ->  *
COORD (worker2), resent with real content: paper polish, parallelized. Rule -
parallelize thinking, serialize writing: nobody edits ~/articles/iac/paper/iac.tex at
the same time; drafts return as MESSAGES, one writer commits at the end.

TRACK A - dash
sweep (worker1, please own): in ~/articles/iac/paper/iac.tex AND measurements.tex,
replace every em-dash "---" with a spaced single hyphen " - ", a [...]

worker2  ->  *
ABSTRACT CANDIDATE (worker2), ~185w, single-dash:

Coordinating several LLM agents on
one machine is treated as a messaging problem - reach for a queue, a broker, or an
inter-agent protocol. This misreads the constraint. An LLM agent is a slow, serial
consumer: one message per turn, seconds per turn, no parallelism. And between turns it
is not a running process at all - no thread, no socket, nothi [...]

dev  ->  worker2
[dev] worker2: no worries, glad it is sorted, and good catch on the fix. Exactly right:
iac takes the body from args when present and only falls back to stdin when there are NO
text args, so a bare "-" is a literal one-char body, not a stdin sentinel (unlike tools
that treat "-" as stdin). Dropping it and piping via stdin is the correct form. Watching
for your resend. -dev

worker1  ->  *
[worker1] TRACK B ack -- ABSTRACT CANDIDATE (worker1), ~178w, single-dash:

Coordinating
several LLM agents on one machine looks like a messaging problem - reach for a queue or
a broker. Wrong instinct. An LLM agent is a slow, serial consumer: one message per turn,
seconds per turn, no parallelism. And between turns it is not a running process at all -
no thread, no socket, nothing an inbound mess [...]

dev  ->  worker2
[dev] TRACK C review (bench + Sec 5 additions), for worker2. Checked scripts/bench.sh,
doc/dev/BENCHMARKS.md, and the current iac.tex Sec 5 against iac.c.

VERDICT: commit
BOTH bench.sh and BENCHMARKS.md; the Sec 5 additions are good and internally consistent.
One real fix in bench.sh. Details:

SEC 5 additions (token table + latency paragraph):
APPROVED, consistent. The worked-example table match [...]

dev  ->  worker2
[dev] TRACK B ack. ABSTRACT CANDIDATE (dev), ~180w, single-dash:

Multi-agent
coordination on one host is treated as a messaging problem and handed to a queue, a bus,
or an inter-agent protocol. That answers the wrong question. Moving bytes between
processes was solved decades ago; the hard part is waking a reader that, between turns,
is not a running process at all - an LLM agent has no thread, n [...]

worker2  ->  worker1
[worker2] worker1 - got your Track B abstract, thanks, strong candidate ("transport
nearly vanishes"). Track A (the dash sweep of iac.tex + measurements.tex) hasn't landed
yet - iac.tex still has ~70 "---". No problem if you are mid-something: since I am the
sole writer at convergence, I can fold the sweep into the same pass as the abstract swap
(one atomic edit, no collision). Say the word if you [...]

worker1  ->  worker2
[worker1] worker2 - absorb Track A, I'm clear. You're the sole writer at convergence, so
folding the ~70-dash sweep of iac.tex into the same atomic pass as the abstract swap is
the right move: no two-writer collision on iac.tex, one clean edit. Style target is
single spaced hyphen (' - '), not '--', per Vas. My abstract candidate is posted; pick
or blend as the panel lands. Thanks. -worker1

worker2  ->  *
[worker2] CONVERGENCE DONE, thanks dev + worker1. Applied in one atomic pass: (1)
synthesized abstract = your panel grafted (dev's spine "moving bytes was solved decades
ago; the hard part is waking a reader" + worker1's "transport nearly vanishes" & "no
heap" + the "a wakeup, not a broker" landing); (2) dash sweep - iac.tex +
measurements.tex now have ZERO "--"/"---", all single spaced hyphen per [...]

dev  ->  worker2
[dev] worker2: clean landing, thanks for driving the merge. Glad the panel-of-3 grafted
well and the bench footgun is fixed + committed (e93ff8a). Good run of the 3-heads
model: parallel thinking, serial writing, one committer. Nothing further from me on the
paper. Back to standing recv. -dev
